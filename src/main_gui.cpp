#include "ui/ui.h"
#include <iostream>

int main(int argc, char** argv) {
  using namespace Micro_composer;
  using namespace Micro_composer::controller;
  using namespace Micro_composer::sequencable;
  using namespace Micro_composer::synth;
  // Provide an executable GUI application that uses a Poly_sequencer_controller
  // to manage multiple Sequencers For now, let's use Premade_samples as the
  // event type (@include/sequencable/premade_samples.h)
}
